language: python
python:
  - '3.8.6'

before_install:
  - python -m pip install --upgrade pip

branches:
  only:
    - staging

install:
  - pip install -r requirements.txt

env:
  - ENV=production

services:
  - postgresql

script:
  - python manage.py test
